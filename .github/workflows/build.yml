name: Build

on:
  pull_request:
    branches: ["main"]
    paths:
      - ".github/workflows/build.yml"
      - "CMakeLists.txt"
      - "cmake/*.cmake"
      - "src/**/*.cpp"
      - "src/**/*.h"
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/build.yml"
      - "CMakeLists.txt"
      - "cmake/*.cmake"
      - "src/**/*.cpp"
      - "src/**/*.h"

jobs:
  linux-build:
    name: Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: "recursive"
      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.10.0"
          target: "desktop"
          arch: "linux_gcc_64"
      - name: Install Build Dependencies
        run: |
          echo Installing CMake...
          sudo apt install cmake -y
          echo Installing GR dependencies...
          sudo apt install autoconf autoconf-archive automake libtool
          sudo apt install libjpeg9-dev libpng-dev zlib1g-dev libqhull-dev libcairo2-dev
          sudo apt install libxt6 libxrender1 libxext6 libglx-mesa0 libqt6widgets6 -y
      - uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: "${{ github.workspace }}/lib/vcpkg"
          vcpkgJsonGlob: "**/vcpkg.json"
      - name: Prepare CMake
        run: |
          cmake -S . -B build
      - name: Build Using CMake
        run: |
          cmake --build build

  windows-build:
    name: Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: "recursive"
      - uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: "${{ github.workspace }}/lib/vcpkg"
          vcpkgJsonGlob: "**/vcpkg.json"
      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.10.0"
          target: "desktop"
          arch: "win64_msvc2022_64"
      - name: Prepare CMake
        run: |
          cmake -S . -B build
      - name: Build Using CMake
        run: |
          cmake --build build
